// prisma/schema.prisma

datasource postgres {
  provider = "sqlite"
  url = "***"
}

model Post {
  id        Int     @id @default(autoincrement())
  title     String
  body      String
  published Boolean
}